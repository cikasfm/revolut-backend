# HikariCP is smart enough to figure out the driver based on JDBC Connection URL
jdbcUrl=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;INIT=runscript from 'classpath:/schema.sql'
maximumPoolSize=100
minimumIdle=1